import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const MoodScanScreen = () => {
  return (
    <View style={styles.container}>
      <Text>MoodScanScreen</Text>
    </View>
  );
};

export default MoodScanScreen;

const styles = StyleSheet.create({
    container:{},
});
